自定义接口输出字段
===================================================

系统提供的列表类接口，一般均支持自定义接口输出字段，用户可根据需求选择字段以便加快数据读取数据


* 调用方式

| 将参数在uri中通过fileds字段传递，选择多个字段用半角逗号 ``,`` 分隔，例如：

.. code-block:: bash

    /api/v1/ip?fields=id,ip,port,hostname
